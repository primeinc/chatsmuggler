# Enforce frozen lockfile — no lockfile mutations during install
frozen-lockfile=true

# Reject installs if pnpm version doesn't match packageManager field
engine-strict=true

# Disallow changing the lockfile on install (belt + suspenders with frozen-lockfile)
lockfile-only=false

# Hoist nothing — keep node_modules clean and explicit
hoist=false
shamefully-hoist=false

# Ensure pnpm is the only package manager (kills npm/yarn fallback)
manage-package-manager-versions=true
